import React from 'react';
import ReactDOM from 'react-dom';
import App from './Components/App';

const main_app = React.createElement(App);
ReactDOM.render(main_app, document.getElementById('root'));
